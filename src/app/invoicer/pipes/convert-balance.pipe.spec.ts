import { ConvertBalancePipe } from './convert-balance.pipe';

describe('ConvertBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
